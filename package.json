{
  "name": "proyecto-cosechando",
  "version": "1.0.0",
  "description": "Proyecto Cosechando - Aplicación web para gestión de productos agrícolas",
  "private": true,
  "scripts": {
    "install": "cd frontend && npm install && npm run build:prod",
    "start": "cd frontend && npm run serve:dist"
  }
}

